---
import BaseHead from '../../astro/components/BaseHead.astro';
import Footer from '../../astro/components/Footer.astro';
import Header from '../../astro/components/Header.astro';
import PostCard from '../../astro/components/PostCard.astro';
import { SITE_TITLE } from '../../consts';
import { getSortedPosts } from '../../lib/posts';

const posts = await getSortedPosts('en');
---

<!doctype html>
<html lang="en">
  <head>
    <BaseHead
      title={`${SITE_TITLE} — English`}
      description="Dev notes on AWS, JVM, Kubernetes, Spring and more."
      alternates={{ ko: '/', en: '/en/' }}
    />
  </head>
  <body>
    <Header locale="en" />
    <main class="max-w-3xl mx-auto px-4 sm:px-6 py-16">
      <section class="mb-16">
        <h1 class="text-4xl sm:text-5xl font-bold tracking-tight text-slate-900 dark:text-slate-100 mb-4">
          Hello, World
        </h1>
        <p class="text-lg text-slate-600 dark:text-slate-400 leading-relaxed">
          Notes on the tech I'm into — AWS, JVM, Kubernetes, Spring and more.
        </p>
      </section>

      <section>
        <h2 class="text-xl font-semibold text-slate-900 dark:text-slate-100 mb-6">Recent posts</h2>
        <ul class="divide-y divide-slate-100 dark:divide-slate-800/70">
          {posts.map((post) => (
            <li class="py-6 first:pt-0 last:pb-0">
              <PostCard post={post} />
            </li>
          ))}
        </ul>
      </section>
    </main>
    <Footer />
  </body>
</html>
